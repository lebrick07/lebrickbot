{
  "name": "{{REPO_NAME}}",
  "version": "1.0.0",
  "description": "{{CUSTOMER_NAME}} application",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
