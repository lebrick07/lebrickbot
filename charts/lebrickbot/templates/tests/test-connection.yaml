apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lebrickbot.fullname" . }}-test-connection"
  labels:
    {{- include "lebrickbot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lebrickbot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
